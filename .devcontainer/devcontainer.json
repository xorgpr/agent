{
    "image":"mcr.microsoft.com/devcontainers/universal:2",
    "postCreateCommand": "mkdir -p ~/.claude-code-router && cp .devcontainer/claude-code-router-config.json ~/.claude-code-router/config.json && sudo npm install -g npm@11.7.0 && sudo npm install -g @anthropic-ai/claude-code && sudo npm install -g @musistudio/claude-code-router",
    "remoteUser": "codespace"
}